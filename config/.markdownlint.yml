default: true
MD013: false  # line-length — URLs longas são inevitáveis
MD024: false  # no-duplicate-heading — headings duplicados entre PT-BR e EN são intencionais
MD033: false  # no-inline-html — necessário para <details>/<summary>
MD051: false  # link-fragments — GitHub gera sufixo -1 para headings duplicados, linter não reconhece
